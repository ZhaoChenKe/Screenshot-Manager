# Instructions for Screenshot-Manager Project

- **Version Increment Rule**: 每次修改或迭代代码时，必须在 `app/build.gradle.kts` 中将版本号递增：
  - `versionCode` 每次 +1（例如 1 -> 2 -> 3）。
  - `versionName` 递增修订版本号（例如 1.0 -> 1.0.1 -> 1.0.2）。
